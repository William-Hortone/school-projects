import React from "react";
import "./header.css";

const Header = () => {
  return (
    <div className="app__header">
      <p>Student</p>
      <button>Logout</button>
    </div>
  );
};

export default Header;
